package com.mytuin.gardenplanner.ui.theme

import androidx.compose.material3.Typography

/**
 * V1_VISUAL_DESIGN_SPECIFICATION §11 approves the standard Android/Compose
 * typography system (Roboto). Material 3's default Typography already uses
 * Roboto on Android, so step 1 adopts it unchanged. A later visual-design
 * pass may override individual roles here without touching screens.
 */
val MyTuinTypography: Typography = Typography()
